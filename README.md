# job-dashboard

Static, data-less front end for a private job scanner. This page contains **no
data**: it loads postings live from a private repo via the GitHub REST API using
a fine-grained token supplied at runtime (kept only in the browser's
localStorage). Nothing in this repository is sensitive.
